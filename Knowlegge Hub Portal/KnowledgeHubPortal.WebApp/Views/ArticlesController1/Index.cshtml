@model IEnumerable<KnowledgeHubPortal.Domain.Entities.Article>

@{
    ViewData["Title"] = "Index";
}

@TempData["Message"]

@* <div class="alert alert-warning alert-dismissible fade show" role="alert">
  <strong>Message</strong> @TempData["Message"]
  <button type="button" class="close" data-dismiss="alert" aria-label="Close">
    <span aria-hidden="true">&times;</span>
  </button>
</div> *@

<h1>Articles</h1>
@* <select>
    @foreach (var cat in ViewBag.Catagories as List<KnowledgeHubPortal.Domain.Entities.Catagory>)
    {
        <option value="@cat.CatagoryId">@cat.Name</option>
    }
</select> *@

@* @Html.DropDownList("Catagories") *@ @*Catagories is a key name in viewbag*@
<form asp-action="Index" asp-controller="ArticlesController1" method="post">
    Select Catagory:
    <select name="cid" asp-items="ViewBag.Catagories">

    </select>
    <input type="submit" name="btnSearch" value="Search"> </input>
</form>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SubmittedBy)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <a href="@item.ArticleUrl" target="_blank">@item.Title</a>
            </td>
            <td>
                @item.Description
            </td>
            <td>
                @item.SubmittedBy
            </td>
            @* <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td> *@
        </tr>
}
    </tbody>
</table>
